// == Import : npm
import React from 'react';
import { Link } from 'react-router-dom';

// == Import : local
import './style.scss';

const Profile = () => {
    return (
        <div className="Profile">
            Profile
        </div>
    );
};

export default Profile;